/* exported last */

// create a function with one parameter
// function must return the last element of the array
// return array at the length of array - 1

function last(array) {
  return array[array.length - 1];
}
