/* exported getFirstChar */

// Create a function with one parameter
// Make sure to return the first character of every string we input
// return string at the first index

function getFirstChar(string) {
  return string[0];
}
